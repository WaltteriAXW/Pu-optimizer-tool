# The physics engine itself is pure standard library — it runs in the browser under
# Pyodide with no packages loaded. These are for running the test suite and any offline
# analysis outside the browser.
numpy>=1.24.0
scikit-learn>=1.3.0

# xgboost and scikit-optimize were listed here for an ML optimizer that trained on data it
# generated from the physics and was never reachable from the application. Both ship in the
# Pyodide distribution, so they can come back with loadPackage() if a model is ever trained
# on real production shots.

# Optional: For advanced features
# pandas>=2.0.0  # For data analysis
# matplotlib>=3.7.0  # For visualization

# Development dependencies
# pytest>=7.4.0  # For testing
# black>=23.0.0  # For code formatting
# pylint>=2.17.0  # For linting
